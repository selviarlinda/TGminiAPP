# ### СЕКРЕТЫ И ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ ###
# Это самая важная часть! Файл .env никогда не должен попадать в Git.
.env
*.env
.env.*
!.env.example

# ### ЗАВИСИМОСТИ NODE.JS ###
# Папки с установленными пакетами для фронтенда и бэкенда.
# Они могут весить сотни мегабайт и устанавливаются командой `npm install`.
node_modules/

# ### СБОРОЧНЫЕ АРТЕФАКТЫ И БИЛДЫ ###
# Папки, которые создаются при сборке проекта (например, React).
# Они генерируются автоматически.
build/
dist/

# ### ЛОГИ ###
# Файлы логов, которые могут создаваться во время работы или отладки.
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ### ФАЙЛЫ ОПЕРАЦИОННОЙ СИСТЕМЫ ###
# Системный мусор от macOS, Windows и Linux.
.DS_Store
Thumbs.db
ehthumbs.db

# ### ФАЙЛЫ IDE И РЕДАКТОРОВ КОДА ###
# Папки с настройками вашей среды разработки (VSCode, WebStorm и т.д.).
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
